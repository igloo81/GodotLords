{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "build",
        "type": "shell",
        "command": "dotnet build",
        "group": {
            "kind": "build",
            "isDefault": true  // This makes the build task the default one when running `Ctrl+Shift+B`
        },
        "problemMatcher": "$msCompile",  // This helps VSCode detect and show build errors
        "detail": "Build the C# project using dotnet build"
    }
  ]
}